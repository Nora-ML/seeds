import Layout from "../components/Layout"


//to pass content ("hello next") in a component it needs to be passed as props.children to the component
const Home = () => <Layout>Hello next</Layout>;

export default Home;